using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum KirbyAction
{
    StartRunning,
    StopRunning,

    StartJumping,
    StopJumping,

    StartAttacking,
    StopAttacking,

    StartGuarding,
    StopGuarding
}
