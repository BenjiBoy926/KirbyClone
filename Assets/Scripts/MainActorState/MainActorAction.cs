using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum MainActorAction
{
    StartRunning,
    StopRunning,

    StartJumping,
    StopJumping,

    StartAttacking,
    StopAttacking,

    StartGuarding,
    StopGuarding
}
